spring:
  datasource:
    url: "jdbc:mysql://localhost/calmdownman"
    username: ""
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false

jwt:
  secret: RequiredSecretKey
  expiration: 3_600_000 # 1시간
  refresh-expiration: 86_400_000 #24시간
  issuer: "(주)금병영"

logging:
  level:
    org.hibernate.orm.connections.pooling: warn